import { Layout } from '@/components/layout/layout';
import { DotGridSpotlight } from '@/ui/dot-grid-spotlight';

export const PlaygroundPage = () => {
    return (
        <Layout>
            <DotGridSpotlight className='fixed inset-0' />
        </Layout>
    );
};
